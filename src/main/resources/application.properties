#MySql configurations


server.servlet.context-path=/api/v1.0

#JPA configuration


spring.jpa.properties.hibernate.format_sql=true

#email configuration
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=${BREVO_USERNAME}
spring.mail.password=${BREVO_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
# it upgrades the connection from plain text to secure TLS/SSL, so your email credentials and content aren?t sent in plain text over the network.
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from=${BREVO_FROM_MAIL}

jwt.secret=2842jhfb2kjn3uihdf8273yfhbsdu8374bhsdbjahs
jwt.expiration=360000
money.manager.frontend.url=${MONEY_MANAGER_FRONTEND_URL}
app.activation.url=${MONEY_MANAGER_BACKEND_URL}

spring.profiles.active=prod





